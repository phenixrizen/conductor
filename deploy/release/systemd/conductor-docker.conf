# Install ONLY for conductor-worker (native graph) and conductor-executor:
# /etc/systemd/system/conductor@conductor-executor.service.d/docker.conf
# Docker daemon access is host administration authority. Dedicated worker hosts
# are required when the control-plane host must not share that authority.
[Unit]
After=docker.service
[Service]
SupplementaryGroups=docker
