export default defineAppConfig({
  ui: {
    // Brand palette from docs/design/brand.md: forest for primary actions,
    // terracotta as the restrained accent. Status colors stay semantic.
    colors: {
      primary: 'forest',
      secondary: 'terracotta',
      neutral: 'zinc',
    },
  },
})
